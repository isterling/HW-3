//Balls, balls everywhere

void setup() {
  background(255);
  size(500,500);

}

void draw() {
  for (int x = 20; x < 480; x = x+25) {
    for (int y = 20; y < 480; y = y+25) {
        noStroke();
        ellipse (x, y, 20, 20);
        fill(0);
    }
  }
}
